# 用户素材接入状态（2026-09-13，V7）

六张透明原图保存在 `source-art/`，构建为 90 个角色姿态与 21 个气泡。实时角色使用 `user-actions.png` 和 `user-actions.json`。旧 V6 黑底分离素材不再参与构建。

使用原始 alpha，按轮廓分配可分离的角色和装饰，连接的区域按审核分栏裁切。保留源图原有边缘，不执行二次去底或重绘。

预览：`qa/user-art-v7/index.html`。验证范围为本地 Qt 渲染、自动测试及启动状态，不代表 TeleAgent 上游会话全链路测试。
