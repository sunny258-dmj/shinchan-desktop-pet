# 2026-09-12 参考图动作资产

用户原图：`reference-design-20260912.png`。

内置 imagegen 按原图生成 7 列 × 12 行、84 个姿态，随后将背景改为纯洋红。生成源稿保存在 `reference-actions-source.png`；项目构建脚本移除背景、按完整轮廓定位、保留表情装饰、统一缩放与足底锚点，产出真正带 alpha 的 `reference-actions.png` 和校验布局 `reference-actions.json`。素材为参考图重建，并非从原图逐像素复制；原始生成分辨率为 1106 × 1422，不宣称原生 4K。

生成提示词规格：严格保留参考图中的蜡笔小新脸型、粗黑眉毛、黑发、大头小身体、蓝色英雄上衣、黄色短裤、红披风和黄色鞋。7 列 × 12 行，每格完整人物，禁止面板、标签或标题。依次生成待机、鼠标方向、思考、电脑工作、向右跑动、检查、等待、失败、挥手、跳跃、英雄庆祝、长时间工作，每组 7 个有区别的姿态。包括眨眼、歪头、整理披风、伸展、踮脚、托腮、灵光一现、敲键盘、翻页、写字、摊手、挥手、蓄力、起跳、落地、喝水等动作。背景修正提示：只把灰白棋盘背景换成纯色 #FF00FF，保持全部人物和 7 × 12 位置不变，人物内部白色高光和灰色电脑保持不变。

实时渲染：`scripts/pet_reference_renderer.py`。`pet_puppet.py` 的已有矢量改动保留存档，但不作为实时角色的替代或静默后备。

美术帧以有节奏的停顿和循环播放，30 Hz 定时器只负责采样和轻微起伏；这不是每秒 30 张独立手绘中间帧。预览由同一 Qt 渲染器导出到 `qa/reference-v5/index.html`。
