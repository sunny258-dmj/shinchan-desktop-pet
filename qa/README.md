# QA 输出目录

V4 的视觉检查文件由 `tests/render_redesign.py`、`tests/render_scenes_v4.py`、`tests/render_smoke.py` 和 `scripts/inspect-assets.py` 在本地生成，不再提交大型 PNG/WebP/ZIP/历史 patch。

这样可以避免旧版 V2/V3 截图与当前实现混淆，也能显著控制技能包体积。
